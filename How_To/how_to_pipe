at the end of each child, just before execve
send pipefd to function
use 'dup' to create new file for the outfile

int	fd_tmp;
if (child != 0 && child != max)
{
	//change write end of the pipe
	fd_tmp = dup(pipefd[1]);
	close(pipefd[1]);
	pipefd = fd_tmp;
}