to make the bonus of : << >> 
	- add "O_APPEND" to the open function call
	- this will write any information at the end of the end

in main -> if here_doc

while (ft_strcmp(line, "LIMITER"))
{
	free(line);
	write "pipe here_doc> " stdout
	gnl(stdin, *line)
	ft_add_tad(**file, line);
}


add limiter to here_doc -> 
if (file[0] == NO_INFILE)
{
	send limiter && pipe[0] to here_doc_manager
	here_doc_manager read stdin, find limiter and put everything inside pipe[0]
	it will also delete first of cmds and make new cmds without limiter
}

    0   |    1     |    2    |  3   |    4    |   5     |
./pipex | here_doc | limiter | cmd1 |  cmd2   | outfile |
./pipex |  infile  |   cmd1  | cmd2 | outfile |